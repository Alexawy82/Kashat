Archived components (not used by current UI). Kept for reference and potential reuse:

- src/components/mobile/SwipeableList.tsx
- src/components/mobile/MobileNavigation.tsx
- src/components/help/HelpSystem.tsx

Retired pages (removed from nav, page files deleted):

- app/detect/page.tsx — utilities moved under Settings → Data Management
- app/test-components/page.tsx — dev/test surface
- app/demo/page.tsx — demo surface

This folder lists what has been archived or retired during cleanup. Do not ship these in production builds unless explicitly reintroduced.
